# Playwright Compare

A Python-based visual comparison tool that captures screenshots and highlights visual differences between reference and live versions of a webpage.

## Setup

pip install -r requirements.txt

## Run
python capture_playwright.py
python compare.py